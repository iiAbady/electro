{#
| Variable         | Type    | Description                     |
|------------------|---------|---------------------------------|
| cart             | object  |                                 |
| cart.items_count | int     |                                 |
| cart.total       | string  | Formatted total ex: "١٠٠ ر.س"   |
#}
{% set important_links = theme.settings.get('important_links') %}
<header class="store-header">
    {# Top Nav #}
	<div class="p-6 flex items-center">
	<h5 class="flex-shrink basis-auto">Free 2-day shippings for orders of $50</h5>
	<div class="flex-1 container flex gap-x-3 justify-end">
	<ul class="flex gap-x-3 list-none after:w-[1px] after:h-[22px] after:bg-gray-500 after:my-auto">
	<li> <a href="">Privacy Policy</a> </li>
	<li> <a href="">Terms and Conditions</a> </li>
	</ul>
	<ul class="flex gap-x-3 list-none">
	<li><a href="tel:123456789"><i class="sicon-phone-talking align-middle"></i> +123456789</a></li>
	<li><a href="mailto:electro@abady.com"><i class="sicon-paper-plane align-middle"></i> electro@abady.com</a></li>
	</ul>
	</div>
	</div>
    {# <div class="top-navbar">
        <div class="container flex justify-between">
            <div class="flex-1 flex items-center rtl:space-x-reverse space-x-2"> #}
              {# Footer Menu #}
              {# {% if important_links %}
                {% component 'footer.menu' with {is_header:true} %}
              {% endif %} #}

                {# Scopes | Branches #}
                {# {% if store.scope %}
                    <button class="btn--rounded-gray"
                            onclick="salla.event.dispatch('scopes::open', {'mode': 'default'})">
                        <i class="sicon-location rtl:ml-1 ltr:mr-1"></i> {{ store.scope.name }}
                    </button>
                {% endif %} #}

                {# Search Component #}
                {# <button onclick="salla.event.dispatch('search::open')" class="btn--circle-gray search-btn">
                <span class="sicon-search"></span>
                <span class="sm:hidden px-2 opacity-50">{{ trans('blocks.header.searchProducts') }}</span>
              </button> #}
              {# <div class="header-search flex-1">
                <salla-search inline oval height="36"></salla-search>
              </div>
            </div>

            {% component 'footer.contacts' with {is_header:true} %}
        </div>
    </div> #}

    {# Main Nav #}
	<div class="container mx-auto">
    <div id="mainnav" class="main-nav-container shadow-default bg-white h-11 pb-16 rounded-2xl static">
        <div class="inner bg-inherit rounded-2xl p-3">
            <div class="container">
                <div class="flex items-stretch justify-between relative gap-x-2">
                    <div class="flex flex-1 items-center">
                        <a class="lg:hidden mburger mburger--collapse leading-none" href="#mobile-menu">
                            <i class="sicon-menu text-primary text-2xl rtl:ml-2.5 ltr:mr-2.5"></i>
                        </a>
                        <a class="navbar-brand" href="{{ store.url }}">
                            <img fetchpriority="high" width="100%" height="100%" loading="eager" src="{{ store.logo }}" alt="{{ store.name }}">
                            {% if is_page('index') %}
                              <h1 class="sr-only">{{ store.name }}</h1>
                            {% else %}
                              <h2 class="sr-only">{{ store.name }}</h2>
                            {% endif %}
                        </a>
			<div class="header-search flex-1">
                <salla-search inline oval height="36"></salla-search>
              </div>
                        {# {% component 'header.menu' %} #}
                    </div>
                    <div class="flex items-center justify-end gap-x-2">
                        {% if user.type=='user' %}
                          <salla-user-menu avatar-only show-header></salla-user-menu>
                        {% else %}
                            <button class="header-btn" onclick="salla.event.dispatch('login::open')">
                              <i class="header-btn__icon sicon-user-circle"></i>
                            </button>
                        {% endif %}
                        <salla-cart-summary class="ml-4 rtl:ml-[unset] rtl:mr-4">
                          <i slot="icon" class="header-btn__icon icon sicon-shopping-bag"></i>
                        </salla-cart-summary>
					<div class="header-buttons ml-3">
                    {# Language & Currency Component #}
                    {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
                        <button type="button" onclick="salla.event.dispatch('localization::open')" class="btn--rounded-gray basis-0 bg-[rgba(245, 245, 245, 1)]">
                            <span class="flag iti__flag iti__{{ user.language.country_code }} rounded-sm"></span>
                            <span class="mx-1.5">|</span> <span>{{ currency.symbol }}</span>
                        </button>
                        <salla-localization-modal>
						</salla-localization-modal>
                    {% endif %}
                </div>
                    </div>
                </div>
				{% component 'header.menu' %}
            </div>
        </div>
    </div>
	</div>
</header>
{% if store.scope %}
    <salla-scopes selection="{{ store.scope.display_as == 'popup' ? 'mandatory' : 'optional' }}"></salla-scopes>
{% endif %}
